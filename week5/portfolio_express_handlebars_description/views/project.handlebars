<nav>
    <div id="menu2">
        <a class="menu-link" href="/">Home</a>
        {{#projects}}
        {{#if selected}}<a class="menu-link highlighted" href="{{directory}}">{{name}}</a>{{else}}
            <a class="menu-link" href="{{directory}}">{{name}}</a>
            {{/if}}
        {{/projects}}
    </div>
</nav>
<div class="container2">
    <h1 class="h1project item">{{currproject.name}}</h1>
    {{>image}}
    {{>descrip}}
</div>